apiVersion: v1
kind: Service
metadata:
  name: inventory
  namespace: semishop
  labels:
    app: inventory
spec:
  selector:
    app: inventory
  ports:
    - name: http
      port: 80
      targetPort: 8085
